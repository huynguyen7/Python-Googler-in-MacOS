# Python Googler in MacOS
A simple command line to run Google Search Query in SHELL, then pop up the result (on a separate tab) in default/running browser on MacOS.

## Dependencies
- Python 3

## Setup
- Make the script executable:
$ chmod +x googler.py
